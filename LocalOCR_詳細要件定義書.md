# Local OCR 詳細要件定義書

- 文書ID: LOC-OCR-REQ-001
- 版: 1.0
- 対象: Windows向けインストール不要Electronポータブル版
- 関連文書:
  - `LocalOCR_開発計画書.md`
  - `LocalOCR_システム設計書.md`
  - `LocalOCR_WBS.md`

---

## 1. 文書の目的

本書は、任意の画像ファイルまたはスクリーンショットを読み込み、日本語・英数字を完全オフラインでOCRし、結果を確認・編集・コピー・保存できるWindowsデスクトップアプリ「Local OCR」の機能要件、非機能要件、画面要件、受入基準を定義する。

---

## 2. 製品概要

### 2.1 製品コンセプト

> 画像をドラッグまたは貼り付けるだけで、外部送信せずに文字を抽出できる、簡潔で安全なローカルOCRアプリ。

### 2.2 主な利用場面

- Windowsのスクリーンショットから文字列を取り出す
- PNG、JPEG等の画像化された文書から文章を抽出する
- クラウドOCRへ送信できない業務画像をローカル処理する
- OCR結果を手修正し、クリップボードやテキストファイルへ出力する
- 複数画像を連続してOCRする

### 2.3 対象OS

- Windows 11を正式対応
- Windows 10は動作確認対象とするが、Microsoftのサポート状況を考慮して保証範囲を別途決定する
- x64を必須対象とする
- ARM64は初期版対象外

### 2.4 配布形態

- インストール不要のポータブル版
- 第一候補: electron-builderのWindows `portable`ターゲットによる単一EXE
- 大容量モデルや起動性能に問題がある場合: 展開済みフォルダ版またはZIP版も併売
- 管理者権限を要求しない
- レジストリへの恒久的な登録を行わない
- 原則としてユーザーの明示操作なしに外部通信を行わない

---

## 3. スコープ

### 3.1 初期版に含める

- 画像ファイル選択
- ドラッグ＆ドロップ
- クリップボード画像貼り付け
- 複数画像の管理
- 画像プレビュー
- 回転、ズーム、範囲選択
- 画像全体OCR
- 選択範囲OCR
- 日本語・英数字OCR
- OCR領域の座標表示
- 信頼度表示
- OCR結果編集
- コピー
- TXT、JSON保存
- 完全オフライン動作
- ポータブル版ビルド
- エラー表示、キャンセル、再実行

### 3.2 初期版に含めない

- PDF直接読込
- Word、Excelへの直接出力
- 検索可能PDF生成
- 表構造の復元
- 手書き文字専用認識
- 数式認識
- 翻訳
- 生成AIによる校正
- クラウドOCR
- フォルダ常時監視
- 自動アップデート
- macOS、Linux対応
- 医療機器または診断用途としての使用保証

---

## 4. ユーザーと利用環境

### 4.1 想定ユーザー

- 一般的なWindows操作ができる業務ユーザー
- OCRライブラリやコマンドラインの知識を持たないユーザー
- オフラインまたは閉域環境で画像を扱うユーザー
- 認識結果を最終確認できるユーザー

### 4.2 前提条件

- Windows上でアプリのEXEを実行できる
- 利用者が対象画像にアクセスできる
- OCR結果には誤認識があり得ることを理解し、重要用途では原画像と照合する
- アプリは抽出結果の正確性を保証しない

---

## 5. 主要ユースケース

### UC-01 画像ファイルをOCRする

1. ユーザーが「画像を開く」を選択する
2. ファイルダイアログで画像を1件以上選択する
3. 画像一覧に追加される
4. 選択画像がプレビューされる
5. ユーザーが「OCR実行」を押す
6. 処理進捗が表示される
7. 認識文字と座標枠が表示される
8. ユーザーが結果を確認・編集する
9. コピーまたは保存する

### UC-02 スクリーンショットを貼り付けてOCRする

1. ユーザーが `Win + Shift + S` 等でスクリーンショットを取得する
2. Local OCRをアクティブにする
3. `Ctrl + V` を押す
4. クリップボードの画像が一覧に追加される
5. 設定に応じて自動OCRまたは手動OCRを行う
6. 結果をコピーする

### UC-03 選択範囲だけOCRする

1. ユーザーがプレビュー上で矩形範囲を指定する
2. 「選択範囲をOCR」を実行する
3. 対象領域だけが切り出されOCRされる
4. 座標は原画像座標へ変換して保持される
5. 結果が既存結果へ置換または追記される

### UC-04 複数画像を一括OCRする

1. 複数画像を追加する
2. 「すべてOCR」を実行する
3. キュー順に処理される
4. 失敗画像があっても残りは継続される
5. 画像ごとの成功・失敗状態が表示される

### UC-05 OCR結果を保存する

1. ユーザーが保存形式を選ぶ
2. 保存先を指定する
3. TXTの場合は表示中の編集済みテキストを保存する
4. JSONの場合は原文、編集済み文、座標、信頼度、処理条件を保存する

---

## 6. 機能要件

### 6.1 画像入力

#### FR-IN-001 ファイル選択
- OS標準ファイルダイアログから1件以上選択できる
- 対応拡張子をフィルタ表示する
- キャンセル時は状態を変更しない

#### FR-IN-002 ドラッグ＆ドロップ
- アプリ全体または指定ドロップ領域で画像を受け付ける
- 非対応形式は追加せず理由を通知する
- 複数ファイルを受け付ける

#### FR-IN-003 クリップボード貼り付け
- `Ctrl + V` でクリップボード内の画像を取得する
- 画像とテキストが同時に存在する場合は画像を優先する
- 画像がない場合は非侵襲的な通知を表示する
- 貼り付け画像には日時ベースの仮名を付ける

#### FR-IN-004 対応形式
初期版で以下を必須とする。

- PNG
- JPEG/JPG
- WebP
- BMP

次は非対応として明示する。

- HEIC/HEIF
- TIFF
- SVG
- GIFアニメーション
- PDF

#### FR-IN-005 入力検証
- ファイル拡張子だけでなくMIMEまたはデコード可否を確認する
- 0バイトファイルを拒否する
- 巨大画像は警告または安全な縮小処理を行う
- 原本は変更しない

### 6.2 画像一覧管理

#### FR-LIST-001 一覧表示
各画像について以下を表示する。

- サムネイル
- ファイル名または仮名
- 画像寸法
- OCR状態
- エラー有無
- 認識文字数

#### FR-LIST-002 並び替え
- 追加順を基本とする
- ドラッグによる手動並び替えはMVP後半で実装可能とする

#### FR-LIST-003 削除
- 選択画像を一覧から削除できる
- 未保存の編集済み結果がある場合は確認する
- 原本ファイルは削除しない

#### FR-LIST-004 全件クリア
- 全画像を一覧から除去できる
- 未保存データがある場合は確認する

### 6.3 画像プレビュー

#### FR-VIEW-001 表示
- 選択画像をアスペクト比を維持して表示する
- 初期表示は「画面に合わせる」

#### FR-VIEW-002 ズーム
- 拡大、縮小、100%、画面に合わせるを提供する
- マウスホイール＋修飾キーによるズームを任意実装する
- ズーム範囲の目安は10%～800%

#### FR-VIEW-003 パン
- 拡大時に画像をドラッグ移動できる

#### FR-VIEW-004 回転
- 90度単位で左右回転できる
- 回転はアプリ内状態に適用し、原本は変更しない
- OCR座標と表示座標の変換を一貫して行う

#### FR-VIEW-005 範囲選択
- 矩形範囲を1件指定できる
- 選択範囲を解除できる
- 選択範囲は画像座標で保持する
- 画像外へはみ出さない

#### FR-VIEW-006 OCR枠
- 認識行または認識単位のポリゴンを重ねて表示する
- 枠表示をON/OFFできる
- 選択中の認識結果を強調する

### 6.4 OCR

#### FR-OCR-001 エンジン
- 第一候補をPaddleOCR.js + ONNX Runtime Webとする
- 日本語モデルと文字検出モデルをアプリへ同梱する
- 技術検証で成立しない場合にPythonサイドカー方式へ切り替える

#### FR-OCR-002 言語
初期版は以下を提供する。

- 日本語＋英数字
- 英語中心

言語モデル自動判定は初期版対象外とする。

#### FR-OCR-003 全体OCR
- 現在画像全体をOCRできる
- 実行前の回転状態を反映する
- OCR開始後に進捗状態を表示する

#### FR-OCR-004 範囲OCR
- 選択矩形を切り出してOCRできる
- 結果座標を原画像座標へ戻す
- 結果の扱いを「置換」「追記」から選べる設計余地を持つ
- 初期版の既定値は「現在結果を置換」

#### FR-OCR-005 一括OCR
- 未処理画像または全画像を対象にできる
- 同時実行数は原則1とし、メモリ安定性を優先する
- 1件の失敗でキュー全体を停止しない
- 全体進捗と個別進捗を表示する

#### FR-OCR-006 キャンセル
- 現在処理または一括キューをキャンセルできる
- Worker終了が必要な場合は安全に再初期化する
- キャンセル済み結果を成功結果として扱わない

#### FR-OCR-007 再実行
- 同じ画像と設定で再実行できる
- 既存の編集済み結果を上書きする前に確認する

#### FR-OCR-008 OCR結果
最低限、次を保持する。

```ts
type OcrBlock = {
  id: string;
  text: string;
  confidence: number | null;
  polygon: Array<[number, number]>;
  order: number;
};
```

画像単位では次を保持する。

```ts
type OcrDocument = {
  imageId: string;
  rawText: string;
  editedText: string;
  blocks: OcrBlock[];
  engine: string;
  model: string;
  language: string;
  durationMs: number;
  processedAt: string;
};
```

#### FR-OCR-009 モデル初期化
- 初回OCR時またはアプリ起動時にモデルを初期化する
- 初期化中であることを明示する
- モデルロード失敗時は原因と復旧手段を表示する
- 再初期化を可能にする

### 6.5 画像前処理

#### FR-PRE-001 プリセット
初期版では以下を提供する。

- なし
- 文書向け
- スクリーンショット向け

#### FR-PRE-002 基本処理
- EXIF回転補正
- デコード時の色空間統一
- 過大画像の安全な縮小
- 透明背景の合成

#### FR-PRE-003 非破壊
- 前処理後画像は一時データとする
- 原本を変更しない
- OCR結果に使用プリセットを記録する

### 6.6 OCR結果表示・編集

#### FR-RES-001 テキスト表示
- OCR全文を編集可能なテキスト領域に表示する
- OCR直後はrawTextとeditedTextを同値とする

#### FR-RES-002 双方向ハイライト
- OCRブロックをクリックすると該当文字列を選択または強調する
- テキスト行を選択した場合、可能な範囲で画像上の枠を強調する
- 完全な文字オフセット対応が難しい場合は行単位対応とする

#### FR-RES-003 信頼度
- ブロックごとの信頼度を表示できる
- しきい値未満を要確認として視覚化する
- 色のみで意味を伝えない

#### FR-RES-004 編集
- ユーザーが自由に修正できる
- 編集済みフラグを保持する
- 「OCR直後へ戻す」を提供する

#### FR-RES-005 整形
以下を個別またはプリセットで提供する。

- 行頭・行末空白除去
- 連続空白整理
- 空行整理
- 改行保持
- 改行を連結
- 日本語文字間の不要な半角空白除去

整形は編集済みテキストへ適用し、rawTextは保持する。

### 6.7 コピー・保存

#### FR-OUT-001 全文コピー
- 編集済みテキストをクリップボードへコピーする
- 成功通知を表示する

#### FR-OUT-002 選択範囲コピー
- テキストエリアで選択した文字列をコピーできる

#### FR-OUT-003 TXT保存
- UTF-8で保存する
- 既定名は元画像名とする
- 貼り付け画像は日時名とする

#### FR-OUT-004 JSON保存
以下を含める。

- スキーマバージョン
- 元画像情報
- rawText
- editedText
- OCRブロック
- 信頼度
- OCR設定
- 処理日時
- 処理時間
- アプリバージョン

#### FR-OUT-005 一括保存
- 初期版では任意
- 実装する場合は画像ごとのTXT/JSONを指定フォルダへ保存する
- 同名衝突を安全に回避する

### 6.8 設定

#### FR-SET-001 一般設定
- 貼り付け後の自動OCR ON/OFF
- OCR完了後の自動コピー ON/OFF
- OCR枠表示 ON/OFF
- 既定前処理プリセット
- 既定言語
- 低信頼度しきい値

#### FR-SET-002 設定保存
- ユーザーごとのローカル設定として保存する
- ポータブルモードではアプリ配下またはElectronのportableデータディレクトリを利用する
- 保存失敗時は既定値で継続する

#### FR-SET-003 リセット
- 設定を既定値へ戻せる

### 6.9 エラー処理

#### FR-ERR-001 入力エラー
- 非対応形式
- 壊れた画像
- 過大画像
- 読み込み権限不足

#### FR-ERR-002 OCRエラー
- モデルロード失敗
- WASMロード失敗
- Worker異常終了
- メモリ不足
- OCR結果ゼロ件

#### FR-ERR-003 保存エラー
- 書込権限不足
- ディスク容量不足
- ファイル使用中
- パス不正

#### FR-ERR-004 ログ
- 個人情報を含み得るOCR本文や画像を既定でログへ出力しない
- エラー種別、スタック、アプリバージョン、環境情報をローカルログへ記録する
- ログ出力を無効化できる余地を持つ

---

## 7. 画面要件

### 7.1 メイン画面

3ペイン構成を基本とする。

```text
┌─────────────────────────────────────────────────────────────┐
│ メニューバー / ツールバー                                  │
├────────────┬───────────────────────────┬────────────────────┤
│ 画像一覧    │ 画像プレビュー             │ OCR結果             │
│            │                           │                    │
│            │                           │ 編集・コピー・保存  │
├────────────┴───────────────────────────┴────────────────────┤
│ ステータス / 進捗 / エラー                                  │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 ツールバー

最低限、以下を配置する。

- 画像を開く
- 貼り付け
- OCR実行
- 選択範囲OCR
- すべてOCR
- キャンセル
- コピー
- 保存
- 設定

### 7.3 レスポンシブ方針

デスクトップアプリであるため、モバイル対応は不要。ただし以下に対応する。

- 最小ウィンドウサイズ: 1024 × 640
- 推奨: 1440 × 900以上
- 狭い場合は右ペインをタブまたは折りたたみにできる
- Windows表示倍率125～200%で操作できる

### 7.4 アクセシビリティ

- キーボードのみで主要操作可能
- フォーカス表示を消さない
- ボタンにツールチップまたはアクセシブル名を付ける
- 色だけで状態を表現しない
- OCR進捗をスクリーンリーダーに通知可能な構造とする
- 最低限のコントラストを確保する

### 7.5 キーボードショートカット

| 操作 | ショートカット |
|---|---|
| 画像を開く | Ctrl+O |
| 貼り付け | Ctrl+V |
| OCR実行 | Ctrl+Enter |
| 全文コピー | Ctrl+Shift+C |
| 保存 | Ctrl+S |
| 選択画像削除 | Delete |
| 画面に合わせる | Ctrl+0 |
| 拡大 | Ctrl++ |
| 縮小 | Ctrl+- |
| 設定 | Ctrl+, |

既存のテキスト編集ショートカットと競合しないようにする。

---

## 8. 非機能要件

### 8.1 セキュリティ

- `nodeIntegration: false`
- `contextIsolation: true`
- preloadの`contextBridge`経由で許可APIのみ公開
- レンダラーから任意のNode APIへアクセスさせない
- IPC引数を検証する
- 任意URLナビゲーションを禁止する
- 新規ウィンドウ生成を原則禁止する
- 外部コンテンツをアプリ内部へ読み込まない
- Content Security Policyを設定する
- モデル、WASM、辞書をローカル同梱する
- OCR画像・結果を外部送信しない
- 自動更新を初期版では実装しない
- 依存パッケージの脆弱性監査をリリース前に実施する

### 8.2 プライバシー

- 画像およびOCR本文を永続保存しない
- 明示保存した結果のみユーザー指定先へ保存する
- 一時データはメモリ内を基本とする
- 一時ファイルを作成する場合は終了時または次回起動時に清掃する
- クラッシュログにOCR本文を含めない

### 8.3 完全オフライン

以下をネットワーク遮断状態で満たす。

- 起動
- モデル初期化
- 画像読込
- OCR
- 編集
- コピー
- TXT/JSON保存

外部CDN、Webフォント、分析SDK、広告SDK、クラウドAPIを使用しない。

### 8.4 性能

初期目標値。

| 項目 | 目標 |
|---|---|
| コールド起動 | 標準業務PCで10秒以内 |
| UI初期表示 | 5秒以内 |
| モデル初回初期化 | 15秒以内 |
| 1920×1080スクショOCR | 10秒以内 |
| A4相当300dpi画像OCR | 20秒以内 |
| OCR中のUI | フリーズせずキャンセル可能 |
| 連続処理 | 20画像をクラッシュせず処理 |
| メモリ | 通常利用で1.5GB未満を目標 |

性能は検証PCの仕様と併記して評価する。

### 8.5 信頼性

- 単一画像の失敗でアプリ全体が終了しない
- 一括処理の他画像を継続できる
- Worker異常終了後に再初期化できる
- 未保存編集がある状態での終了時に確認する
- 設定ファイル破損時は既定値で起動する
- モデルファイル欠損時は明確なエラーを表示する

### 8.6 保守性

- TypeScript strictを有効にする
- OCRエンジンをインターフェースで抽象化する
- UIとOCR処理を分離する
- IPCチャンネル名を定数化する
- 入出力スキーマにバージョンを付ける
- 主要ロジックに単体テストを設ける
- E2Eテストで基本導線を検証する

### 8.7 可搬性

- ZIP展開後、任意の書込可能フォルダから起動できる
- 管理者権限不要
- ユーザー名に日本語が含まれるパスで動作する
- パスに空白が含まれても動作する
- オフラインPCへUSB等で持ち込める構成とする
- ネットワークドライブ上の実行は保証対象外としてよい

---

## 9. OCR精度要件

OCR精度は画像条件に依存するため、単一の絶対精度を保証しない。代わりに、標準評価セットと比較基準を設ける。

### 9.1 評価セット

最低50画像。

- Windows UIスクリーンショット
- 白背景の日本語文書
- 写真撮影した文書
- 小さい文字
- 英数字混在
- 表を含む画像
- 縦書き参考画像
- 低コントラスト画像

### 9.2 指標

- 文字誤り率 CER
- 単語または行単位の一致率
- 読み順の妥当性
- 処理時間
- クラッシュ率
- 低信頼度と誤認識の相関

### 9.3 合格基準

- 標準的な横書きスクリーンショットおよび文書画像で、ベースライン候補より明確に劣らない
- 重要な日本語、英数字混在で実用的な修正量に収まる
- 20画像連続処理で異常終了しない
- 精度不足の場合、Python版PaddleOCRサイドカーへの切替判断を行う

---

## 10. データ要件

### 10.1 内部データ

```ts
type ImageItem = {
  id: string;
  displayName: string;
  sourceType: "file" | "clipboard";
  originalPath?: string;
  mimeType: string;
  width: number;
  height: number;
  rotation: 0 | 90 | 180 | 270;
  status: "idle" | "loading" | "ready" | "processing" | "success" | "error" | "cancelled";
  dirty: boolean;
  error?: AppError;
  ocr?: OcrDocument;
};
```

### 10.2 JSON出力スキーマ

```json
{
  "schemaVersion": "1.0",
  "appVersion": "0.1.0",
  "source": {
    "displayName": "sample.png",
    "width": 1920,
    "height": 1080
  },
  "ocr": {
    "engine": "paddleocr-js",
    "model": "configured-model-id",
    "language": "ja",
    "preprocessPreset": "screenshot",
    "durationMs": 4321,
    "rawText": "認識結果",
    "editedText": "修正後の認識結果",
    "blocks": []
  }
}
```

---

## 11. 受入試験

### AT-001 ファイルOCR
- PNGを選択できる
- OCRが完了する
- テキストが表示される
- コピーできる

### AT-002 スクリーンショット貼り付け
- Windowsで取得した画像を`Ctrl+V`で追加できる
- OCRできる
- 元画像ファイルを必要としない

### AT-003 完全オフライン
- ネットワークアダプター無効状態で起動・OCR・保存が成功する
- 外部通信エラーが発生しない

### AT-004 ポータブル起動
- ZIP展開のみで起動できる
- 管理者権限を要求しない
- 別フォルダへ移動しても起動できる

### AT-005 複数画像
- 10画像を追加できる
- 一括OCRできる
- 1画像を意図的に失敗させても他画像を処理する

### AT-006 範囲OCR
- 矩形選択できる
- 選択範囲だけがOCRされる
- 表示枠が原画像の正しい位置に重なる

### AT-007 編集保護
- OCR結果編集後に再OCRすると確認が出る
- 未保存結果を持つ画像削除時に確認が出る

### AT-008 保存
- TXTがUTF-8で保存される
- JSONが仕様通り出力される
- 日本語ファイル名・日本語パスで保存できる

### AT-009 セキュリティ設定
- nodeIntegrationが無効
- contextIsolationが有効
- rendererからNode.js APIを直接参照できない
- 任意外部URLへ遷移しない

### AT-010 異常復旧
- OCR Workerを異常終了させてもUIが落ちない
- 再初期化後に別画像のOCRが成功する

---

## 12. リリース判定基準

以下をすべて満たした時点でMVPリリース可能とする。

- 必須機能の受入試験が合格
- オフライン試験が合格
- 主要なCritical/High不具合が0件
- 20画像連続OCRでクラッシュしない
- ポータブルビルドをクリーンWindows環境で起動できる
- ライセンス表示を同梱している
- OCR結果の免責と原画像確認の注意書きがある
- 依存パッケージの脆弱性確認を完了している

---

## 13. 未決事項

- PaddleOCR.jsで採用する日本語モデルの具体的な識別子と配布条件
- ONNX/WASMの読み込み方式
- Worker内でのOCR実行可否と最適構成
- 単一EXE内にモデルを圧縮するか、外部resourcesへ配置するか
- Windows 10を正式保証するか
- コード署名の有無
- 自動コピーの既定値
- OCR結果と画像座標の双方向選択粒度
